#include <apr.h>

/*
 * TODO: Put APR/APEX translations shared by multiple shim modules here once
 * their public types and ownership rules have been settled.  Backend-specific
 * helpers currently stay in the source file which owns them so that enabling
 * the APR shim does not require Matrix653 headers on a development host.
 */
